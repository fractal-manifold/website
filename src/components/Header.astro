---
---

<header>
  <div class="container">
    <nav>
      <a href="/" class="logo">
        <img src="/logo.png" alt="Fractal Manifold" class="logo-img" />
        <span class="logo-text">Fractal Manifold</span>
      </a>
      <div class="nav-links">
        <a href="#startups">Startups</a>
        <a href="#research">Research</a>
        <a href="#contact">Contact</a>
      </div>
    </nav>
  </div>
</header>

<style>
  header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: rgba(10, 10, 10, 0.8);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(243, 158, 11, 0.1);
  }

  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 0;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 1rem;
    text-decoration: none;
    transition: opacity 0.3s ease;
  }

  .logo:hover {
    opacity: 0.8;
  }

  .logo-img {
    height: 45px;
    width: auto;
    display: block;
  }

  .logo-text {
    font-family: var(--font-display);
    font-size: 1.3rem;
    font-weight: 400;
    letter-spacing: 1px;
    background: linear-gradient(135deg, var(--color-gold) 0%, var(--color-gold) 30%, var(--color-purple) 70%, var(--color-purple) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    white-space: nowrap;
    line-height: 1.2;
  }

  .nav-links {
    display: flex;
    gap: 2rem;
  }

  .nav-links a {
    color: var(--color-text-secondary);
    font-weight: 500;
    transition: all 0.3s ease;
    position: relative;
  }

  .nav-links a::after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--color-gold) 0%, var(--color-gold) 30%, var(--color-purple) 70%, var(--color-purple) 100%);
    transition: width 0.3s ease;
  }

  .nav-links a:hover {
    color: var(--color-text);
  }

  .nav-links a:hover::after {
    width: 100%;
  }

  @media (max-width: 768px) {
    nav {
      padding: 1.25rem 0;
    }

    .logo {
      gap: 0.75rem;
    }

    .logo-img {
      height: 32px;
    }

    .logo-text {
      font-size: 1rem;
    }

    .nav-links {
      gap: 0.875rem;
    }

    .nav-links a {
      font-size: 0.875rem;
    }
  }

  @media (max-width: 480px) {
    nav {
      padding: 1rem 0;
    }

    .logo {
      gap: 0.5rem;
    }

    .logo-img {
      height: 28px;
    }

    .logo-text {
      font-size: 0.85rem;
      white-space: normal;
      max-width: 120px;
    }

    .nav-links {
      gap: 0.625rem;
    }

    .nav-links a {
      font-size: 0.8rem;
    }
  }
</style>
