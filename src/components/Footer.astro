---
---

<footer>
  <div class="container">
    <div class="footer-content">
      <div class="footer-brand">
        <h3>Fractal Manifold</h3>
        <p>Investment & AI Innovation</p>
      </div>
      <div class="footer-contact">
        <a href="mailto:contact@fractalmanifold.com" class="email">
          contact@fractalmanifold.com
        </a>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; {new Date().getFullYear()} Fractal Manifold. All rights reserved.</p>
    </div>
  </div>
</footer>

<style>
  footer {
    background: var(--color-bg-secondary);
    border-top: 1px solid rgba(243, 158, 11, 0.1);
    margin-top: 5rem;
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 3rem 0;
    border-bottom: 1px solid rgba(243, 158, 11, 0.05);
  }

  .footer-brand h3 {
    background: linear-gradient(135deg, var(--color-gold) 0%, var(--color-gold) 30%, var(--color-purple) 70%, var(--color-purple) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .footer-brand p {
    color: var(--color-text-muted);
  }

  .email {
    color: var(--color-gold);
    font-size: 1.1rem;
    font-weight: 500;
    padding: 0.75rem 1.5rem;
    border: 1px solid var(--color-gold);
    border-radius: 8px;
    transition: all 0.3s ease;
  }

  .email:hover {
    background: var(--color-gold);
    color: var(--color-bg);
    transform: translateY(-2px);
    box-shadow: 0 4px 20px rgba(245, 158, 11, 0.3);
  }

  .footer-bottom {
    padding: 2rem 0;
    text-align: center;
  }

  .footer-bottom p {
    color: var(--color-text-muted);
    font-size: 0.9rem;
  }

  @media (max-width: 768px) {
    .footer-content {
      flex-direction: column;
      gap: 2rem;
      text-align: center;
    }
  }
</style>
